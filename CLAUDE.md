# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 仓库性质

这不是代码仓库，是一套**中文文档交付物**：面向某软件公司质量测试部的智能体评测体系建设材料。没有构建产物、没有依赖清单、没有运行时。读者是部门里的测试工程师和测试开发工程师，交付要求是"拿到就能照着建"。

```
智能体评测体系/         ← 唯一的交付物
  00-总纲.md ~ 09-模板与附录.md    10 份分册，约 7100 行（基线版）
  assets/*.svg                     16 张手写 SVG（md 用；HTML 已重画为内联图）
  智能体评测体系.html              增订版单文件 HTML，18 章 / 29 图 / 97 指标卡
相关论文/               ← 5 篇来源论文的中文精读笔记（引用时的事实依据）
大厂案例/               ← 11 条业界案例的原文存档，英文已全文中译（README.md 是台账）
平台PRD/                ← 评测平台的产品需求文档（方案：自研中枢 + Langfuse 作底座）
业界大厂智能体评测案例.txt / 智能体评测相关开源项目.txt   ← 素材清单
```

`平台PRD/` 是**方法论的落地载体**，不重新定义方法论：每条功能需求都标注了追溯到 10 分册的出处章节，改需求前先读出处。其中关于 Langfuse 的每条能力断言都注明了代码依据（文件路径 / 枚举名 / 字段名），见 PRD 附录 16.1——**不要用文档或博客里的说法覆盖这些断言，要改先回代码核**。

⚠️ 该 PRD 涉及 DeepSeek 与 Langfuse 的密钥。**本仓库是公开仓库，任何提交物中只允许出现环境变量名，绝不写入真实密钥**；提交前扫一遍 `sk-[A-Za-z0-9]{16,}`。

`相关论文/` 是**引用纪律的依据**：文中每个实测数字都应能在这 5 篇里找到出处。要引用新数字时先读这些笔记，不要凭印象写。

`大厂案例/` 与 `相关论文/` **口径不同，不要混用**：前者是厂商博客/技术报告/媒体报道的原文存档，引用时只能标注为「业界公开报道」；后者是论文级实证，可作为实测数据支撑阈值与结论。`09` 分册的参考索引已经拆成两张表，新增引用时对号入座。

## 校验与编辑规则（按需加载）

- **改完文档要跑校验** → `Skill(ageval-doc-check)`
  （5 项不变量的做法与基线数字；写校验脚本踩过的三个坑都会表现成「文档有问题」的假阳性）
- **编辑 00-09 分册、加新图、改指标或打分公式之前** → `Skill(ageval-editing-rules)`
  （文档体系的两条正交轴与依赖方向、rubric 硬规则、跨册必须同步的数字、绘图约定、写作口径）

**两条红线常驻**：指标条目数不要手写，用 `01` 里的正则数（md 基线现为 12 组 **92** 个，
出现在 `01` 两处与 `09` 参考索引共三处，改一处必须同步全部）；
新增图必须遵守 `00` §10 的绘图约定。

## md 基线版 与 HTML 增订版的关系

`智能体评测体系.html` 不是 10 份 md 的渲染，是**在其上重编的增订版**，两者内容已不完全相同，这是设计如此：

| | md 10 份分册 | `智能体评测体系.html` |
|---|---|---|
| 定位 | **基线版**，方法论的正式载体 | **增订版**，图表化重编 + 吸收 19 条业界案例 |
| 指标数 | 92 | **97**（基线 92 + 新增 C-09 / D-07~D-09 / J-09） |
| 章节 | 10 册 | 18 章（`01` 拆成「怎么选」与「字典」；`09` 拆成模板/表单/附录；新增汇报视图、阅读指南、业界对照三章） |
| 图号 | `图 0-1`~`图 9-3`，65 条 | `图 <章>-<序>`，29 张。**两套号不互通**，映射表在 HTML 的 §1.3 |
| 绘图 | mermaid + `assets/*.svg` | 全部内联 SVG，零硬编码颜色 |

**跨册同步的 5 组数字（α/β/τ、试次 k、门禁时长、L-01、L-02）两版必须一致，改动时两处都要改。** 指标数不一致是预期内的，不要"修正"。

HTML 增订版的改动后校验（都是实测项，不接受目测）——脚本一次性写，不进仓库：

- **静态**：内部锚点 0 悬空 · id 唯一 · 0 外链与 `@import` · SVG 结构（viewBox 宽=1000、无 width/height、有 role+aria-label、**无硬编码颜色**、每个 `<text>` 带 `on-*` 底色类）· marker id 唯一 · 图号按章连续无重复 · 密钥扫描 · 20 条内容不变量断言 · 指标计数取 `.mtx` 实际数 · **社区来源引用处不得出现百分比或「实测」**
- **浏览器**：离线 0 外部请求 · 明暗双主题 WCAG **0 失败** · 375/768/1280/1440 四宽度 `scrollWidth ≤ innerWidth`（`.tw` 与 `.fig-frame` 的内部横滚是设计，不算）· 交互逐个点过 · **切暗色再打印预览**，表格卡片不得深底

⚠️ 对比度必须用 computed styles 实算，截图不可靠。两套规则：HTML 文本取 `color` + 沿祖先链找背景（**半透明要 alpha 合成**，斑马纹是 42% 透明）；SVG 文本取 **`fill` 不是 `color`**，背景**不做几何推断**，靠 `<text>` 上的 `on-*` 类查令牌——这就是那个类存在的唯一理由，删了对比度就没法自动校验。

⚠️ 五层色阶明暗两套的混色比例不同（浅色 8/16/26/38/52%，暗色 8/15/23/32/42%），且**色带上的文字一律 `--ink` 不降调**。实测过：10.5px 的 `--ink-3` 压在 L3 以上色带，明暗都过不了 4.5:1。

## 本仓库的 git 情况

- 远端 `github.com/kuangaiyong/agent_evaluation`，主分支 `main`，当前工作分支 `dev/agent-eval-system`
- **本地没有配置 git user.name / user.email**，直接 commit 会失败。历史提交用的是 `卡布 <290417990@qq.com>`。不要擅自改 git config，让用户自己设。
- 全局 CLAUDE.md 的规则 4/7 要求"代码内版本号 + tag + Release 三处一致"，但本仓库是纯文档、没有代码版本号——发版时以 tag 与 Release 两处一致为准，需要时先与用户确认。
