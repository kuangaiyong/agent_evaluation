## 1. 对齐工具与基线

- [x] 1.1 写 `docs/compare-with-prototype.js`：从 `docs/prototype.html` 与 `frontend/src/` 两侧各自抽取导航结构（一级分组 → 二级菜单 → 路由）、页签清单、表格列名，逐项比对并打印差异；验证方式为在未改动的当前代码上运行，能复现出已知的 8 处差异（接入中心页签、评估任务页签顺序与数据集列、Bad Case 两列、数据中心四列、系统管理页签与五列、实验与回归一列、缺 Audit 页面、数据中心菜单项数）
- [x] 1.2 在脚本注释中写明已知抽取局限（只识别静态声明的 `el-table-column` / `el-tab-pane`，动态生成的列会漏检），并让脚本以非零退出码表示存在差异，便于后续当门禁用

## 2. 后端补口径

- [x] 2.1 `models.App` 新增可空的 `channel` 字段，并在 `db.ensure_schema()` 补幂等增量 DDL；验证方式为对存量库执行后列存在且原有行不受影响
- [x] 2.2 应用读接口在返回中补 `type` 与 `channel`，`channel` 为空时按 `AgentScope → 通道①`、`OpenCode → 通道②`、其余 → 通道③ 推导；补单元测试覆盖三条推导分支与显式赋值优先于推导
- [x] 2.3 `/api/system/audits` 增加 `actor` 查询参数与由 `action` 派生的 `high_risk` 布尔字段（改评分器 rubric、改金标准、重跑评估任务三类为真）；补测试覆盖按操作人过滤、高风险判定、两者叠加取交集
- [x] 2.4 跑全量后端测试确认无回归（当前基线 42 项通过）

## 3. 导航结构调整

- [x] 3.1 `MainLayout.vue` 按 `specs/navigation` 的表调整分组与二级菜单：数据中心合并为单项「轨迹库 / 数据集」，系统管理新增「操作审计」；验证方式为对比脚本的导航部分差异清零
- [x] 3.2 `router.js` 新增 `/audit` 路由；移除数据中心的 `?tab=` 双入口，页内 tab 状态改为可选 query 参数
- [x] 3.3 移除 `isActive()` 中针对 datacenter 的特判，改为统一的路由前缀匹配；验证方式为进入 `/traces/:id` 时「AI Agent 可观测」仍高亮，进入 `/datacenter` 时对应项高亮且同一时刻只有一项高亮
- [x] 3.4 抽出统一的 `canWrite` / `canAdmin` 取值来源供各页复用（只统一取值，不引入权限指令或高阶组件）；验证方式为以只读角色登录后，各页写操作控件全部置灰

## 4. 新建操作审计页面

- [x] 4.1 新建 `frontend/src/pages/Audit.vue`：操作人 / 操作 / 对象 / 结果 / 时间五列，高风险操作带显著标记；验证方式为对照原型 audit 屏，列名与标记一致
- [x] 4.2 接入按操作人筛选下拉与「仅高风险」开关，两者叠加取交集，并显示命中数与空态；验证方式为切换筛选后行数变化符合预期，无匹配时显示空态而非留白
- [x] 4.3 从 `System.vue` 移除「审计日志」页签；验证方式为系统管理页只剩「成员与角色」「工作空间」两个页签，对比脚本该项差异清零

## 5. 逐页对齐（每页改完跑一次对比脚本，确认该页差异清零）

- [x] 5.1 `Access.vue`：接入指引页签改为 LoongSuite 三通道（Python Agent / Pilot / OTLP 直推），每条通道给出可复制的命令与环境变量名；应用列表新增「智能体类型」「接入通道」两列
- [x] 5.2 `Access.vue`：对不在 Pilot 官方支持清单内的工具给出明确提示并指引走通道 ③，不把名称相近但实为不同产品的工具标为已支持
- [x] 5.3 `Tasks.vue`：页签顺序改为「离线任务」在前，离线表新增「数据集」列
- [x] 5.4 `BadCases.vue`：列表新增「触发评估器」「Root Cause」两列，未复核行的 Root Cause 显示为待复核
- [x] 5.5 `Datacenter.vue`：数据集页签新增「类型」「版本」「条目数」「已配金标准」列，金标准未配齐的显式标出；轨迹库行提供「转为评测任务」入口，转换后进入待补录队列
- [x] 5.6 `System.vue`：成员表新增「来源」「可做什么」「加入时间」列，工作空间表新增「日额度」「我的角色」列
- [x] 5.7 `Regression.vue`：对比报告表新增「主要退化原因」列
- [x] 5.8 `Dashboard.vue` / `Traces.vue` / `TraceDetail.vue` / `Metrics.vue` / `Evaluators.vue`：对照原型逐屏核对字段口径，只改不一致处，不做结构性改动

## 6. 交互基线落地

- [x] 6.1 各页主操作按钮接上真实行为（弹窗或明确反馈），确认无哑按钮；验证方式为逐页点击每个按钮，均有可见响应
- [x] 6.2 列表页的筛选控件真实过滤并联动命中计数与空态，关键数值列可排序；验证方式为逐页操作筛选与排序，行数与顺序变化符合预期
- [x] 6.3 可下钻的表格行点击后打开详情，且详情内容取自该行；验证方式为随机抽查三个模块，详情标题与行首列一致

## 7. 整体验收

- [x] 7.1 运行对比脚本，导航结构、页签、表格列三类差异全部清零
- [x] 7.2 运行全量后端测试，保持 42 项以上通过且无回归
- [x] 7.3 起真实服务（后端 + 前端开发服务器），以管理员与只读两种角色各走一遍 11 个二级菜单，确认菜单包含关系、页面可达、写操作按角色置灰
- [x] 7.4 逐屏对照 `docs/prototype.html` 人工过一遍，记录仍存在的偏差与原因（对比脚本抽不到的动态列在此兜底）
