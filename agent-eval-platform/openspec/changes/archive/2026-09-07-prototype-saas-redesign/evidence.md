# 执行证据

命令统一为：`cd agent-eval-platform/docs && node prototype.smoke.js`

**改动前基线**：旧 `prototype.smoke.js` 实测 `✅ 通过 62 / 62`，退出码 0。
（项目 CLAUDE.md 记载的「28 项」为过时数字，Task 10 更正。）

---

## Task 1: 设计 token、外壳骨架与 20 屏导航

**过程中的两处偏差，已就地修正并记录：**

1. **tasks.md 里的断言自身有缺陷** —— 「暗色块内只覆写 token」原写法
   `darkBlocks.join("").split(";").every(d => d.startsWith("--"))`，
   而 `darkBlocks` 含选择器与大括号，split 后第一段必然是 `[data-theme="dark"]{--color-primary:...`，
   断言恒假。改为先截取 `{}` 内的声明再判断。
2. **「19 屏」是我在 G2 阶段的计算错误** —— 菜单分组逐项相加为 2+3+5+3+2+5 = **20** 个导航项。
   菜单结构未变，仅数字写错。已同步修正 proposal / design / spec / tasks / smoke 五处。
   `[data-screen]` 容器合计 21 个（20 导航屏 + trace 下钻屏）。

因**断言被修改**，四次执行全部重跑（旧断言的红-绿证明的不是现在这份测试）：

1. 红：`❌ 通过 11 / 29`，18 条失败，exit=1
   > ⚠️ **更正（Task 4 时发现）**：当时写的命令是 `git show HEAD:docs/prototype.html`，
   > 但仓库根是 `agent_evaluation/`，正确路径应为 `agent-eval-platform/docs/prototype.html`。
   > 该命令实际失败，`>` 把 `prototype.html` 截成了**空文件**，这次「红」是用空文件跑出来的，
   > 不是用旧版原型。红的结论本身成立（功能确实缺失），但原描述与事实不符，特此更正。
2. 绿（新原型）：`✅ 通过 29 / 29`，exit=0
3. 反向（把 `.btn:hover{background:var(--color-muted)}` 改成 `#EEEEEE`）：
   `✗ 组件规则内硬编码颜色 1 处（期望 0）#EEEEEE` → `❌ 通过 28 / 29`，exit=1 ✓ 精确命中目标断言
4. 还原后绿：`✅ 通过 29 / 29`，exit=0

**评审（自审，未起子代理）**：diff 是全新文件而非改动既有逻辑，且约束已被 29 条断言覆盖，
起子代理的冷启动成本收不回来。逐条过异常路径 / 边界 / 绕过路径 / 验收条款，**发现 2 条，均已修**：

- **`go(id)` 传入不存在的 id 会把每一屏都设为 hidden → 白屏**。已加前置校验，未知 id 直接返回。
  这条断言没覆盖（测试只点合法导航项），属自审捡漏。
- **`.mask` 用了 `rgb(15 13 31 / .45)` 字面量，违反 spec「组件内不得出现 hex 或 rgb() 字面量」**。
  测试的正则只查 `#hex`，抓不到 `rgb()`。已提为 `--color-scrim` token（明暗各一份）。
  ⚠️ **`rgb()` 字面量目前只靠人工审保证，Task 10 的静态校验补自动断言。**

修的是实现不是测试，故只重跑一次绿：`✅ 通过 29 / 29`，exit=0。

**Deferred：0 条。**

---

## Task 2: 空间/角色/主题三切换器与通用交互基础设施

1 红：`❌ 通过 36 / 40` —— `受角色管控的元素 0 个（期望 ≥8）`、`弹窗触发器 0 个（期望 ≥5）`，exit=1
2 绿：`✅ 通过 40 / 40`，exit=0（role-only 元素 8 个、弹窗触发器 6 个）
3 反向（注释掉 `applyRole` 里的 `el.hidden = allow.indexOf(role) === -1;`）：
  `✗ 只读角色下泄漏的写操作入口 8 个（期望 0）` → `❌ 通过 39 / 40`，exit=1 ✓ 精确命中
4 还原后绿：`✅ 通过 40 / 40`，exit=0

**记一处红阶段的观察**：`Esc 能关闭全部弹窗（0/0）` 与 `只读角色下泄漏 0 个` 在红阶段是**空集造成的假绿**
（0 === 0、空数组 filter 后仍为空）。底线由 `>= 8` 与 `>= 5` 两条计数断言守住，实现后它们转为真断言（6/6、8 个）。

**评审（自审，diff 约 120 行 < 150 行阈值）**：

- 事件委托顺序核对：document 上三个 click 监听器（`[data-go]` → `closeMenus` → `[data-dlg]`）。
  `data-dlg` 分支里的 `stopPropagation()` 不影响同元素上的其他监听器，
  故一个按钮同时带 `data-go` + `data-dlg` 时会先切屏再开弹窗 —— 这正是 Task 5「发起评估」所需的行为。
- `openDlg` 先把所有 `[data-mask]` 置 hidden 再开目标，避免两个弹窗叠加后 Esc 只关掉一个、
  另一个永久留在遮罩下。
- 点遮罩空白处关闭用 `e.target.hasAttribute("data-mask")` 判定，点弹窗本体不误关。
- 边界：`openDlg` 未知 id、`bindDrop` 元素缺失均已 return 保护。

**已修 0 条**（本任务未发现需当场修的缺陷）。
**Deferred 1 条**：弹窗未做焦点捕获（focus trap）与打开时的初始焦点。原型层面 Esc + 关闭按钮已覆盖主要键盘路径，
生产实现时需补。spec 未要求，Task 10 的 a11y 校验也不覆盖此项。

---

## Task 3: 接入中心四通道

1 红：`❌ 通过 41 / 49` —— `接入通道页签 0 个（期望 4）` 等 8 条失败，exit=1
2 绿：`✅ 通过 49 / 49`，exit=0（四通道齐备、通道取值 `Python Agent/Pilot/OTLP 直推/eBPF`）
3 反向（注释掉 `switchTab` 里的 `pane.hidden = ...` 一行）：
  `✗ 切换通道只显示对应指引（1/4）` → `❌ 通过 48 / 49`，exit=1 ✓ 精确命中
4 还原后绿：`✅ 通过 49 / 49`，exit=0

**本任务顺带产出**：通用页签机制 `switchTab(group, val)` + `PANE` 映射表（group → 面板属性后缀），
后续屏的页签直接复用，不再各写一套。

**评审（自审）**：

- 四条通道的上报端点统一为 `https://otlp.tageneval.internal`，无任何云厂商域名；
  鉴权文案为「接入 Token」，`LicenseKey` 零出现（断言已覆盖）。
- eBPF 面板显式写了能力边界：「只能采集进程外可见的请求与响应，拿不到框架内部的 Agent 决策上下文；
  需要完整轨迹时叠加 Python Agent 或 Pilot」—— 避免读者误以为 eBPF 能替代探针。
- 应用列表的「接入通道」列取值被断言约束在四通道枚举内，防止后续填数据时写出第五种叫法。

**已修 0 条 / Deferred 0 条。**

**工具坑复现**：用 Bash heredoc 写含成对单引号的 Python 脚本再次触发
`unexpected EOF while looking for matching '`（与 Task 1 的反斜杠问题同源）。
改为先 Write 脚本文件再执行。已记入记忆 `feedback-bash-heredoc-backslash`。

---

## Task 4: 链路观测与链路详情下钻

**本任务发现两个测试自身的缺陷，都靠反向验证暴露出来：**

1. **一条断言永远不会红**。原测试点 Trace 列表第 1 行下钻，而详情页 `data-slot="trace-key"` 的
   静态默认值恰好就是第 1 行的 ID（`a3f2c81d`）—— 即使完全不回填也照样通过。
   第一次反向验证注释掉回填逻辑后 **仍是 58/58 全绿**，正是这条测试没在挡事的证明。
   改为**点第 2 行**（`7b91e04a`）后，反向验证才真正转红。
2. **测试在功能缺失时会崩溃而非失败**。用 git 旧版原型跑红时，脚本崩在
   `TypeError: Cannot read properties of undefined (reading 'getAttribute')`（`spaces[1]` 为 undefined），
   崩溃点之后的断言一条都没跑到 —— 这是「判不了」，却很容易被读成「没问题」。已给取值加 null 保护。

因断言被修改，四次执行全部重跑：

1 红（`git show HEAD:agent-eval-platform/docs/prototype.html`，1556 行旧版）：
  37 条失败，**脚本崩溃 0 次**（保护生效，测试完整跑到尾），
  含 `✗ 可下钻的 Trace 行 0 条`，exit=1
2 绿：`✅ 通过 58 / 58`，exit=0
3 反向（把回填的 `n.textContent = key;` 改成 `0;`）：
  `✗ 详情标题带被点行的 Trace ID（7b91e04a）` → `❌ 通过 57 / 58`，exit=1 ✓ 这次真的红了
4 还原后绿：`✅ 通过 58 / 58`，exit=0

**评审（自审）**：

- 下钻用 `data-drill="<screen>:<key>"` + `data-drill-title`，回填到详情页的 `[data-slot="<screen>-key/-title"]`；
  返回按钮记录来源屏（`backTo`），从别的屏下钻进来也能回到正确的列表。
- Token 趋势图用了两条系列，按 a11y 约束叠加了 `stroke-dasharray` 区分输入/输出，并配了
  `<details>` 数据表 fallback —— 不只靠色相。
- 边界：`drillRows[1]` 不存在时断言给出「(无空间切换器)」一类可读信息而非崩溃。

**已修 2 条（均为测试缺陷）/ Deferred 0 条。**

---

## Task 5: 数据魔方三屏

1 红：`❌ 通过 59 / 75`，16 条失败，**脚本崩溃 0 次**，exit=1
2 绿：`✅ 通过 75 / 75`，exit=0
3 反向（把「发起评估」按钮的 `data-dlg="dlg-newtask"` 去掉，只保留 `data-go="tasks"`）：
  `✗ 「发起评估」跳到评估任务并打开新建弹窗` → `❌ 通过 74 / 75`，exit=1 ✓ 精确命中
4 还原后绿：`✅ 通过 75 / 75`，exit=0

**本任务顺带产出**：通用「单选卡片」机制 `[data-pick="<group>"]`，作用域限定在所在弹窗内
（`c.closest("[data-mask]") || document`），避免两个弹窗里同名分组互相干扰。

**评审（自审）**：

- 「发起评估」按钮同时挂 `data-go="tasks"` + `data-dlg="dlg-newtask"`，依赖 Task 2 已验证的
  「同元素两个委托监听器先后执行」行为，一次点击完成跨屏跳转 + 开弹窗。反向验证确认这条链路真的在工作。
- 轨迹库把「Trace 与 eBPF 两路独立清洗后共同写入」写成了页面上的说明文案，
  而不只是埋在数据里 —— 这是理解数据流的关键前提。
- 数据集详情的字段名用小写 `id/input/output/expected_output/trajectory`，与评估任务的变量名对齐，
  避免同一概念在两屏用两种写法。

**已修 0 条 / Deferred 0 条。**

---

## Task 6: 评估器（DeepEval 预置 + 自定义 + 配置表单）

1 红：`❌ 通过 77 / 92`，15 条失败（16 个类名全缺、分组页签 0 个、六个表单字段全缺、公式与映射全缺），
  **崩溃 0 次**，exit=1
2 绿：`✅ 通过 92 / 92`，exit=0（18 行评估器：16 个 DeepEval 预置 + 2 个自定义示例；分组页签 6 个）
3 反向（把 `threshold` 默认值从 `0.75` 改回 DeepEval 的 `0.5`）：
  `✗ 阈值默认 0.5（期望 0.75，非 DeepEval 默认 0.5）` → `❌ 通过 91 / 92`，exit=1 ✓ 精确命中
4 还原后绿：`✅ 通过 92 / 92`，exit=0

**这条反向验证挡的正是最容易犯的错**：直接照搬 DeepEval 默认阈值 0.5，
与平台 τ=0.75 口径不符 —— 同一批样本会评出完全不同的通过率，且不会有任何报错。

**本任务顺带产出**：`filterRows(rowAttr, val)` —— 有些页签不是切面板而是过滤同一张表的行，
与 `switchTab` 分离，evgroup 组走这条路径。

**评审（自审）**：

- 16 个类名与 Context7 查到的 DeepEval 官方文档逐字核对（RAG 5 / Agentic 3 / 多轮会话 4 / 安全与质量 4），
  未凭印象编造类名。
- 三分量映射卡片挂了 `data-map-status` 徽标写「待确认」，正文另有一段 `note-warn` 明说
  「该映射由平台侧提出，尚未经方法论确认，落地前需与评测体系负责人对齐后再固化」——
  避免这份原型被当成已定口径引用。
- 表单里 `threshold` 的 hint 直接写出「DeepEval 的默认值是 0.5，此处已按平台口径覆盖」，
  让看原型的人知道这个差异是有意为之而非笔误。
- 打分公式、α/β/τ/k、三口径全部照抄外层方法论文档，原型只消费不改写。

**已修 0 条 / Deferred 0 条。**

---

## Task 7: 评估任务、分析洞察与 Bad Case

1 红：`❌ 通过 92 / 112`，20 条失败（分步表单、四种数据来源、三口径、三列分数、Bad Case 两列全缺），
  崩溃 0 次，exit=1
2 绿：`✅ 通过 112 / 112`，exit=0
3 反向（删掉弹窗底部的「上一步」按钮）：
  `✗ 「上一步」切回第 1 步` → `❌ 通过 111 / 112`，exit=1 ✓ 精确命中
4 还原后绿：`✅ 通过 112 / 112`，exit=0

**本任务顺带产出**：分步表单机制 `[data-stepgo]` → `[data-step]` 面板 + `[data-stepdot]` 指示器，
作用域限定在所在弹窗内。

**评审（自审）**：

- **三口径不是并排摆着好看**：分析洞察页给了一段明写的解读 ——
  「Average 0.83 看着达标，但 pass^k 只有 0.67，说明重复跑三次有三分之一会掉出阈值，
  这是稳定性问题不是能力问题」。只报 Average 会掩盖它，这正是三口径存在的理由。
- **明细表演示了归一化的必要性**：五行样本分别用 5 分制、Pass/Fail、0-1 连续三种制式打分，
  三列（原始分数 / 分数范围 / 归一化分数）并存，最后一行演示 s_safety 乘法门控 ——
  原始分 0.91 很高，安全门控为 0，最终判 0。这比抽象描述更能说清公式。
- 评分分布图上画了 τ=0.75 的红色虚线判定线，并标注文字，不只靠颜色传达。
- 低分样本下钻取第 2 行（沿用 Task 4 的教训，避开首行默认值巧合）。

**已修 0 条 / Deferred 0 条。**

---

## Task 8: 实验与回归（bullet 图）+ 持续优化两屏

**先修了一处测试缺陷**：原断言「Agent 资产含 Prompt 与 Skill」「经验与记忆两类均呈现」
只查屏内是否出现某个词，而 Task 2 加的操作栏（「新建 Prompt」）与屏描述里本来就有这些词 ——
**两屏一行内容不写也能通过**。第一次跑红时【19】三条全绿，正是这个假绿。
改为断言：页签数量 = 2、页签切换后面板互斥、表格含「版本」列、Latest/Production 双通道、
表格实际行数 ≥ 3。改后重跑红，8 条如期失败。

1 红：`❌ 通过 115 / 126`，11 条失败（门禁 3 条 + 持续优化 8 条），崩溃 0 次，exit=1
2 绿：`✅ 通过 126 / 126`，exit=0（图表增至 5 张，资产 8 行、经验记忆 7 行）
3 反向（删掉 bullet 图的 `<details data-chart-table>` fallback）：
  `✗ 缺数据表 fallback 的图表 1 个（期望 0）` → `❌ 通过 125 / 126`，exit=1 ✓ 精确命中
4 还原后绿：`✅ 通过 126 / 126`，exit=0

**评审（自审）**：

- **bullet 图按 ui-ux-pro-max 的 "Performance vs Target (Compact)" 选型**：灰底轨 = 满分范围、
  彩条 = 实测值、黑色竖线 = 基线 v11、红色虚线 = τ=0.75 判定线。四个元素同图，
  一眼看出「工具调用正确性 0.71 既低于阈值、又比基线退了 0.09」。
- 门禁结论没有只给一个「阻断」徽标，而是写清了判定依据：
  「工具调用正确性 0.71 低于 τ=0.75，且较基线下降 0.09；按『任一核心指标低于 τ 即阻断』执行」。
  看原型的人能理解规则，而不只是看到结果。
- 条形颜色按是否达标切换（`--st-pass` / `--color-destructive`），但**同时有数值标注与虚线位置**，
  不单靠颜色传达达标与否。
- 资产表的 Latest / Production 两列用不同徽标色区分，体现「开发中版本」与「线上生效版本」可以不同 ——
  这是灰度与回滚的前提。

**已修 1 条（测试假绿）/ Deferred 0 条。**

---

## Task 9: 治理与系统五屏 + 总览与快速启动

1 红：`❌ 通过 129 / 145`，16 条失败，崩溃 0 次，exit=1
2 绿：`✅ 通过 145 / 145`，exit=0
3 反向（注释掉「仅看高风险」的行过滤逻辑）：
  `✗ 「仅看高风险」生效：6 → 6 行` → `❌ 通过 143 / 145`，exit=1 ✓ 精确命中
  （连带 `剩余行均为高风险` 也红，因为未过滤时低风险行仍在）
4 还原后绿：`✅ 通过 145 / 145`，exit=0

**评审（自审）**：

- **风险审计与操作审计确实分立**，不是一屏两页签：前者是 Agent 运行时的安全事件
  （提示注入 / 敏感信息泄露 / 越权工具调用），后者是平台内的人为操作。
  两者的责任人、处置流程、留存要求都不同，合并会让安全事件淹没在日常操作里。
- 操作审计把「改评估器阈值 / 改金标准 / 重跑任务」标为高风险并写明理由：
  这三类直接影响评测结论。审计表里有一行演示了「改阈值 τ 0.75 → 0.70」——
  这正是评测体系最怕的静默篡改。
- 配额超额行挂 `data-quota-over` 且用 destructive 语义色，同时给出百分比数字（107%），
  不单靠颜色。超额文案明确写「新任务阻断」而非笼统的「已超额」。
- 成员表的「角色」列取值被断言约束在四种角色内；空间设置卡片整体挂 `data-role-only="admin"`。
- 未读徽标做在顶栏（`[data-unread]`），「全部标为已读」同时清徽标、清小红点、改条目状态，
  三处一起变，避免只清了徽标而列表里还是未读样式。

**已修 0 条 / Deferred 0 条。**

---

## Task 10: 通用表格交互、静态校验与收口

1 红：`❌ 通过 154 / 159`，5 条失败（排序方向 null、行顺序未变、筛选无效、分页无效），崩溃 0 次，exit=1
  —— 【23】可访问性 7 条在红阶段就全绿，因为那些约束在 Task 1-9 实现时已逐步守住，
  本任务只是给它们补上自动断言。
2 绿：`✅ 通过 159 / 159`，exit=0
3 反向（注释掉排序的 `rows.forEach(r => tb.appendChild(r))`，只改箭头不重排 DOM）：
  `✗ 排序真实改变了行顺序` → `❌ 通过 158 / 159`，exit=1 ✓ 精确命中
  （这正是「排序是装饰」的典型形态：箭头会动，数据不动）
4 还原后绿：`✅ 通过 159 / 159`，exit=0

**补上了 Task 1 的 deferred 项**：`rgb()` 字面量的自动断言已加入【23】，
不再只靠人工审 —— 实测组件规则内 `rgb()` 0 处。

### 最终验收

| 项 | 期望 | 实测 |
|---|---|---|
| `node prototype.smoke.js` | 0 FAIL，PASS ≥ 150 | **159 / 159**，exit=0 |
| 禁用词 `AgentLoop / 数据中心 / SLS / 云监控 / MSE / ARMS / AgentSpace / aliyuncs / AI 积分 / LicenseKey` | 各 0 | **全部 0** |
| 保留项 `TAgentEval / 数据魔方 / LoongSuite / OpenTelemetry / DeepEval / eBPF` | 各 ≥1 | 11 / 2 / 2 / 2 / 2 / 10 |
| 图标按钮 aria-label 缺失 | 0 | 0（共 7 个） |
| 表格未包滚动容器 | 0 | 0（共 27 个） |
| 运行时错误 | 0 | 0 |
| 后端回归 | 不受影响 | **75 passed**，0 failed |

> ⚠️ **基线数字更正**：tasks.md 的验收条款写「后端 42 项通过」，实测为 **75 项**。
> 42 是项目 CLAUDE.md 记载的旧基线，工作树里已有 phase1 新增的测试文件
> （`test_access_and_audit.py`、`test_ensure_schema.py`、`test_regression_compare.py`、`test_review_fixes.py`）。
> 本次未动 `backend/`，75 passed / 0 failed，判定为**不受影响**。

### 已同步的 CLAUDE.md（4 处）

1. 原型「14 屏 / 28 项断言」→「20 导航屏 + 1 下钻屏 / 159 项断言」
2. 架构主线「指标字典是单向同步的只读实体」→ 标注作废，改为 DeepEval 口径，并点明
   `metric-dictionary` 能力域与 `sync_metrics.py` **尚未跟进改造**
3. `sync_metrics.py` 的命令注释加了「已失去依据」警示
4. 接入通道「LoongSuite 三通道」→「四通道」（新增 eBPF 无侵入）

**文件规模**：`prototype.html` 1775 行、`prototype.smoke.js` 477 行。

**已修 0 条 / Deferred 0 条**（Task 1 的 1 条 deferred 已在本任务补齐；Task 2 的 focus trap 仍 deferred，见下）。

---

## 收口：浏览器实测（jsdom 之外的视觉验证）

jsdom 只能证明「点得动」，证明不了「长得对」。用 Playwright + 真实 Chrome 打开
`file:///.../prototype.html` 做了一轮实测，**发现两个断言覆盖不到的视觉缺陷并修复**：

1. **所有 SVG 图表被容器拉伸导致比例失控**。viewBox 是 480×160（3:1），
   而 `.chart-box svg{width:100%}` 让它在 1400px 宽的容器里放大近 3 倍 ——
   文字、条形、阈值线全部按比例放大，门禁页的 bullet 图看起来像放在放大镜下。
   修法：`max-height:200px`，让 SVG 按 `preserveAspectRatio` 居中缩放。
2. **工作台下半页大片空白**。补了「最近评估任务」表（含 Average / pass@k / pass^k 三列与门禁结论）。

改的是实现不是测试，重跑一次绿：`✅ 通过 159 / 159`。

### 浏览器实测结果

| 项 | 期望 | 实测 |
|---|---|---|
| 外部请求域名 | 仅 Google Fonts | `fonts.googleapis.com`、`fonts.gstatic.com`，无其他 |
| 页面运行时错误 | 0 | **0 条** |
| 横向溢出 375 / 768 / 1024 / 1440 | 均无 | `scrollWidth == innerWidth`，**四个宽度全部 ✓** |
| 明暗双主题 | 均可用 | 各截图核对，暗色下文字与卡片对比正常 |

截图留在 `docs/preview/`（12 张，明色 8 屏 + 暗色 3 屏 + 弹窗 1 张），供不方便打开原型的人直接看效果。
